# Real Gold Corpus

Place manually annotated events from real economic reports here. Keep raw source documents outside the gold labels when licensing requires it.

Target: 200–500 events across multiple countries, indicators, periods, observed/estimated/forecast values, comparisons and multi-value sentences.

Do not modify labels after seeing a model prediction without recording the annotation revision separately.
